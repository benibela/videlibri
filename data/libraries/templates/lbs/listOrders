<t:switch-prioritized>
<FORM id="reservationsForm" name="borrowerForm" >{cancel-form:=form(., {"cancel": "Vormerkung stornieren"})}
  <DIV class="bodydiv">
    <TABLE class="resultset">
 
         
          <TR>
            <td class="rec_checkbox">
              <input type="checkbox" name="volumeReservationsToCancel">{book:={"cancelId": @value}}</input>
            </td>
            <TD class="rec_title">
              <DIV>
                <TABLE class="titlelist">
                  <TR>
                    <TD class="rec_data">
                      <SPAN class="titleLine">{
                        book.title := extract(.,  "^[^/]*"),
                        book.author:= extract(.,  "/([^(]*)", 1),
                        book.year  := extract(.,  "/.*\((.*)\)", 1)
                        }</SPAN>
                    </TD>
                  </TR>
               <!-- </TABLE>
                <TABLE class="titlelist"> ?? on the page, fails template-->
                  <tr><!--Vormerkungsdatum-->{book.status := ., book.statusId := "ordered"}</tr>
                  <TR>
                    <TD class="rec_data"/>
                    <TD class="rec_data">
                      <SPAN>{book.id := .}</SPAN>
                    </TD>
                  </TR>
                  <!--row: theke: theke ; row : Ausleihfrist in Tagen : 	170 -->
                </TABLE>
                
              </DIV>
            </TD>
          </TR>+
       
 
    </TABLE>
  </DIV>
</FORM>



	<div class="header">
		Keine Vormerkungen vorhanden.
	</div>
</t:switch-prioritized>
