<?xml version="1.0" encoding="UTF-8"?>
<library-template>
<username matches="^(\d000\d{3}|\w\d{8})$"/>
<password matches="."/>
<maxExtendCount value="3"/>
<accountExpired matches="Benutzerkarte ist abgelaufen"/>
<maxlimit matches="Maximal erlaubte Anzahl Verlängerungen"/>

<action id="connect">
  <page url="http://$server;WebOpac.cls" templateFile="start"/>
</action>

<action id="update">
  <page url="http://$server;WebOpac.cls" templateFile="update"
    post-params="MGWCHD=0&amp;TOKEN=$token;&amp;TOKENX=$tokenx;&amp;DATA=$data;&amp;usercode=&amp;VERSION=$version;&amp;RSN=0&amp;TOKENZ=$token;&amp;ACTION=MEMLOGIN&amp;password=$password;&amp;usernum=$username;&amp;login=LOGIN"/>
    <error templateFile="loginError"/>
</action>

<action id="extend-list" singleBookStr="&amp;$book.extendID;=1" update-before="true" update-after="true">
  <page url="http://$server;WebOpac.cls" 
    post-params="MGWCHD=0&amp;TOKEN=$token;&amp;TOKENX=$tokenx;&amp;DATA=$data;&amp;usercode=&amp;VERSION=$version;&amp;ACTION=MEMSLFISS&amp;bno=$username;&amp;mid=$mid;$book-list;"/>
</action>
</library-template>