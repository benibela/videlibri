<html>
<template:meta default-text-matching="regex"/>
<DIV id="right">
  <DIV class="inside">
      <DIV>
        <P>
          <SPAN>(Gesucht|Treffer).*:<t:s>
            let $temp := extract(., "([0-9]+)[^0-9]+([0-9]+)[^0-9]+([0-9]+)$", (1,2)) ! xs:integer(.) return ( 
              $firstIndex := $temp[1],
              $lastIndex := $temp[2](:,
              $search-result-count := $temp[3]:))</t:s></SPAN>
        </P>
      </DIV>
  </DIV>
</DIV>


<DIV id="main">
  <DIV class="inside">
    <DIV class="block">
    
    <div class="toolbar_div"><ul class="toolbar_ul"><li class="toolbar_li">
      <span>Vorwärts</span>
      {form := form(//form)}
    </li></ul></div>
    
    
    </DIV>
  </DIV>
</DIV>   

</html>