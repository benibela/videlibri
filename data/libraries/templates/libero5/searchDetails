<html>

  <DIV class="DisplayRecord">
   <t:loop><t:switch>
      <DIV class="CatInfoCont">
        <TABLE>
          <t:loop><t:switch>
            <TR><TH>T I T E L</TH><TD>{$book.title}</TD></TR>
            <TR><TH>Autor</TH><TD>{$book.author}</TD></TR>
            <TR><TH>Erscheinungsjahr</TH><TD>{$book.year}</TD></TR>
            <TR><TH>ISBN</TH><TD>{$book.isbn}</TD></TR>
            <TR><TH>Link</TH><TD><a><t:read var="book.Link: {replace(., '[.]', '')}!" source="x'{@href}'" /></a></TD></TR>
            <TR><TH>{currentfield := replace(normalize-space(.), "[.]", "")}</TH>
                <TD><t:read source="if (./a/contains(@href, '://')) then x'{.} ( {./a/@href} )' else ." var="book.{$currentfield}!"/></TD></TR>
          </t:switch></t:loop>
        </TABLE>
      </DIV>

      <DIV class="ItemInfoCont">
        {id := 1,$first-input := ()}
        <TABLE><TBODY>
        <TR><t:read source="." var="book.Exemplar {$id}!"/>{id := $id+1, if ($book._order-id) then () else $book._order-id := .//input/@name}</TR>*
        </TBODY></TABLE>
        <a title="Reserve Title">{book._order-url := @href, book.orderable := true()}</a>?
        <div class='SubmitIssuesReserves' t:test="not($book.orderable)">
        {$order-form := form(ancestor::form[1], (.//input)), book.orderable := true()}
        </div>
      </DIV>
   </t:switch></t:loop>
  </DIV>

</html>