<html>
<!--<template:meta default-text-matching="regex"/>-->
  <DIV id="tab-content">
    <TABLE class="data">
    <TR><TH/></TR>
    <TR>
      <t:switch-prioritized>
        <form t:condition="contains(@action, 'availability.do') ">
          {temp := form(.)}
          <t:read var="book.orderTitle{$book.orderable}" source="."/>
          <t:read var="book.order{$book.orderable}" source="$temp.url"/>
          <t:read var="book.orderPost{$book.orderable}" source="$temp.post"/>
          {book.orderConfirmationOptionTitles := (), book.orderable := $book.orderable + 1}
        </form>
        <a t:condition="contains(@href, 'availability.do') and contains(@href, 'katkey')">
          <t:read var="book.orderTitle{$book.orderable}" source="if (. eq 'auswählen') then 'vormerken' else ."/>
          <t:read var="book.order{$book.orderable}" source="@href"/>
          {book.orderConfirmationOptionTitles := (), book.orderable := $book.orderable + 1}
        </a>
      </t:switch-prioritized>
    </TR>*
    </TABLE>
  </DIV>?
</html>
