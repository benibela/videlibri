<html>
<body>
  <div id="mainnav">
     <a template:condition="contains(@href, 'userAccount.do')"><template:read source="@href" var="jsession" regex="jsessionid=[0-9a-zA-Z_-]*"/></a> <!-- jsession can be empty?-->
  </div>
  <input type="hidden" name="CSId">
     <template:read source="@value" var="CSId"/>
  </input>
  <select id="selectedViewBranchlib">{home-branches := option, home-branch-values := option/@value}</select>?
  <select id="selectedSearchBranchlib">{search-branches := option, search-branch-values := option/@value, search-branch-name := @name}</select>?
  <select id="Suche in Zweigstelle">{search-branches := option, search-branch-values := option/@value, search-branch-name := @name}</select>?
</body>
</html>
  
